Kör lokalt genom:
`cd mammas-recept`

följt av

`npx serve .`

Git precommit hook säkerställer att vi trycker in en ny version som används vid inläsning av skript för att säkerställa att vi inte har cache-problem i webbläsaren med gamla cacheade versioner av skript etc.
